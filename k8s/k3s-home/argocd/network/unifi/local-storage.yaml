apiVersion: v1
kind: PersistentVolume
metadata:
  name: local-unifi-pv
  namespace: network
  labels:
    app.kubernetes.io/name: unifi
spec:
  capacity:
    storage: 1Gi
  volumeMode: Filesystem
  storageClassName: local-unifi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  local:
    path: "/data/unifi"
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - k3s-home
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: local-unifi-pvc
  namespace: network
  labels:
    app.kubernetes.io/name: unifi
spec:
  storageClassName: local-unifi
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
