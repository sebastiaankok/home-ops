---
name: Build CI Image

on:
  push:
    branches:
      - main
    paths:
      - k8s/images/ci-image/**

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: gha-runner-scale-set-home-ops
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Set image tag
        id: tag
        run: echo "tag=${GITHUB_SHA::8}" >> "$GITHUB_OUTPUT"

      - name: Build and push
        env:
          REGISTRY_PASSWORD: ${{ secrets.GITHUB_TOKEN }}
        run: |
          sudo -E /kaniko/executor \
            --context="${GITHUB_WORKSPACE}/k8s/images/ci-image" \
            --dockerfile="${GITHUB_WORKSPACE}/k8s/images/ci-image/Dockerfile" \
            --destination="ghcr.io/sebastiaankok/home-ops/ci-image:${{ steps.tag.outputs.tag }}" \
            --registry-credential="ghcr.io=${{ github.actor }}:${REGISTRY_PASSWORD}"
